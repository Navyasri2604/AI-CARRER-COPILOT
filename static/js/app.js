document.addEventListener('DOMContentLoaded',()=>{const root=document.documentElement;const toggle=document.getElementById('themeToggle');const saved=localStorage.getItem('career-theme');if(saved==='light'){root.dataset.theme='light';document.body.classList.add('light')}toggle?.addEventListener('click',()=>{const light=document.body.classList.toggle('light');root.dataset.theme=light?'light':'dark';localStorage.setItem('career-theme',light?'light':'dark');toggle.querySelector('i').className=light?'bi bi-moon':'bi bi-sun'});document.querySelectorAll('[data-count]').forEach(el=>{const target=Number(el.dataset.count);let value=0;const step=Math.max(1,Math.ceil(target/50));const timer=setInterval(()=>{value=Math.min(target,value+step);el.textContent=value.toLocaleString()+(target<100?'%':'');if(value>=target)clearInterval(timer)},24)});document.querySelectorAll('[data-loading]').forEach(form=>form.addEventListener('submit',()=>{const button=form.querySelector('button');if(button){button.disabled=true;button.innerHTML='<span class="spinner-border spinner-border-sm me-2"></span>Working...'}}))});
